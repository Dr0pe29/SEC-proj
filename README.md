# sec-proj

Java Maven workspace with a minimal Hello World app.

## Build

Run `mvn test` to compile and execute tests.

## Run

Run `mvn -q exec:java -Dexec.mainClass=com.example.App` after adding the exec plugin if desired.
